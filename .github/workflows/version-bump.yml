name: Version Bump

# Runs on Forgejo Actions when a pull request into main is merged.  Finds the
# latest v{major}.{minor}.{patch} tag, increments the minor, sets the patch to
# the merged PR number, then creates and pushes the new tag.  MinVer (configured
# in Directory.Build.props) turns that tag into the assembly / installer version.
#
# Requires a registered Forgejo Actions runner with the `ubuntu-latest` label and
# the instance configured to resolve `actions/*` (e.g. from github.com or a
# mirror).  The job token needs contents:write to push the tag.

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write

jobs:
  tag-version:
    if: github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine latest version tag
        id: latest
        run: |
          # Find the latest v{major}.{minor}.{patch} tag reachable from HEAD
          latest_tag=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -n1)
          if [ -z "$latest_tag" ]; then
            echo "major=1" >> "$GITHUB_OUTPUT"
            echo "minor=0" >> "$GITHUB_OUTPUT"
            echo "No existing version tags found - starting at 1.0"
          else
            version="${latest_tag#v}"
            major=$(echo "$version" | cut -d. -f1)
            minor=$(echo "$version" | cut -d. -f2)
            echo "major=$major" >> "$GITHUB_OUTPUT"
            echo "minor=$minor" >> "$GITHUB_OUTPUT"
            echo "Latest tag: $latest_tag (major=$major, minor=$minor)"
          fi

      - name: Compute new version
        id: new
        run: |
          major=${{ steps.latest.outputs.major }}
          minor=$(( ${{ steps.latest.outputs.minor }} + 1 ))
          patch=${{ github.event.pull_request.number }}
          new_version="${major}.${minor}.${patch}"
          echo "version=$new_version" >> "$GITHUB_OUTPUT"
          echo "New version: v$new_version"

      - name: Create and push tag
        run: |
          git config user.name "forgejo-actions[bot]"
          git config user.email "forgejo-actions[bot]@noreply.code.hannahvernon.com"
          git tag -a "v${{ steps.new.outputs.version }}" -m "Release v${{ steps.new.outputs.version }} (PR #${{ github.event.pull_request.number }})"
          git push origin "v${{ steps.new.outputs.version }}"
